Import( 'env', 'libs' )

sources = [ 
	"main.cpp", 
	"SystemTest.cpp",
	"TestDatabase.cpp",
	"../../../../Syncro Server/src/SyncroDB.cpp"
	]

output = env.Program( target = 'SystemTest', source = sources )

env.Install( '../bin/', output )
