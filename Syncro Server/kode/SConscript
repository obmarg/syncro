Import( 'env', 'libtargetdir' )

sources = [ 
	'src/Database.cpp', 
	'src/utils.cpp', 
	'src/tcpconnection.cpp', 
	'src/stopwatch.cpp',
	'src/db/utils.cpp',
	'src/db/statement.cpp'
	 ]

output = env.StaticLibrary( 'kode', sources )

env.Install( libtargetdir ,output )
